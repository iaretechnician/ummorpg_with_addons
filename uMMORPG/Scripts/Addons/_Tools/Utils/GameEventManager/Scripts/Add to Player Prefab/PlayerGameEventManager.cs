//using Mirror;
//using UnityEngine;

//public class PlayerGameEventManager : NetworkBehaviour
//{
    /*

    [Header("[ Component ]")]
    public Player player;

    [Header("[-=-=-[ Game Event Callback ]-=-=-]")]
    public GameEvent updateBuffsList;
    public GameEvent updateSkillsList;
    public GameEvent updateMap;

    void OnSkillhanged(SyncList<Skill>.Operation op, int index, Skill oldData, Skill newData)
    {
        Debug.Log("Skill update required");
        //titleUpdate.TriggerEvent();
        //onTitleLearnedChanged.Invoke();
    }
    public void Awake()
    {
        if (!isServer && !isClient) return;
        player.skills.skills.Callback += OnSkillhanged;
    }*/
//}
